# 上游功能与当前桌面的对应关系

本快照的 Clavis 已合并上游到 `eb9e724`，包括此前基线之后的网格启动器、调色板响应式
布局、独立横幅/头像选择窗口，以及壁纸选择器关闭后重新打开时的网格布局修复。
准确的个人分支提交见 `sources.lock.json`。

| 功能 | 入口或状态 |
|---|---|
| 网格应用启动器 | 设置 → 常规 → Spotlight → 应用 → 布局 → 网格；仍可选列表 |
| 显示器与 Gamma | 设置 → 常规 → 显示器，内含显示器配置与 Gamma 控制两个页面 |
| 夜间模式 | 右侧快捷设置；旧布局会在运行时补入 night 按钮 |
| 左右侧栏位置 | 设置 → 常规 → 侧边栏，可调整内容所在边缘 |
| 灵动岛歌词 | 命令 `qs -c clavis ipc call keystone lyrics`，可在快捷键设置里绑定；无默认快捷键 |
| 桌面卡片 | 从抽屉拖到桌面后，抽屉仍保留，属于个人分支改动 |
| 独立 Dock | 本仓库中的 nyx-dock，配置入口为设置 → 常规 → 下方 Dock |
| 云备份/上传 | 已启用 rclone 网盘管理、电脑备份和灵动岛上传；在设置 → 高级 → 网盘添加账号，账号凭据不入库 |
| 键盘锁定状态 | 当前 Fedora 的 key doctor 报告无法读取部分 LED 设备；属于权限条件，不是缺失 UI 代码 |

日常录屏、音频、剪贴板等命令在本次检查中都已找到。权限、硬件能力、软件安装和用户
选项会影响某些入口的可用性；迁移脚本不自动授予原始键盘读取或扩展性能采样权限。
上游视频若演示的是 dev/实验分支，功能可能尚未进入 main，需要按具体功能再核对。
