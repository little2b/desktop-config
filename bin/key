#!/bin/sh
# Arch package supplies the backend; only the user's Clavis native plugins are local.
export CLAVIS_DISABLE_CLOUD=1
export QML_IMPORT_PATH="$HOME/.local/lib/clavis-runtime/lib/qml${QML_IMPORT_PATH:+:$QML_IMPORT_PATH}"
exec /usr/bin/key "$@"
